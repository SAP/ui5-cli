sap.ui.define([
	"sap/ui/core/mvc/Controller"{{#if modules}},
	{{#each modules}}
	{{#if @last}}"{{name}}"{{else}}"{{name}}",{{/if}}
	{{/each}}
 	{{else}}
	 
	{{/if}}
], function(Controller{{#if modules}}, {{#each modules}}{{#if @last}}{{module}}{{else}}{{module}}, {{/if}}{{/each}}{{/if}}) {
	"use strict";
	return Controller.extend("{{controllerName}}", {
	});
});
